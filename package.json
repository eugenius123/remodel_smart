{
  "name": "remodel",
  "version": "1.0.0",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-img": "^1.0.0",
    "html-minifier": "^4.0.0",
    "image-size": "^1.0.1"
  }
}
